#include <stdio.h>
#include <math.h>

int main(){
	
	int a, b, r, s;
	scanf("%d %d %d %d", &a, &b, &r, &s);
	
	if(a >= 0 && b > 0);
	
	if((a % b == r) || (a % b == s)) printf("YES");
	else printf("NO");	
	
	
	
	return 0;
}